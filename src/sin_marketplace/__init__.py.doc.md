# Purpose: Package initialization for sin_marketplace
# Docs: __init__.py.doc.md

## What this file does

Declares the public API of the `sin_marketplace` package.
Exports the four core classes: `Catalog`, `Installer`, `Registry`, `Updater`.

## Dependencies

- None (stdlib only)

## Usage examples

```python
from sin_marketplace import Catalog, Installer, Registry, Updater
```

## Known caveats

- `__version__` is hardcoded; update it before releasing.
